/**
 * Метка снимка, ещё не загруженного на сервер.
 *
 * Отдельным крошечным модулем, потому что её знают обе стороны: форма
 * (`components/journals/queued-photos.ts`, работает и на дашборде) и
 * очередь кабинета (`journal-queue.ts`). Импортировать очередь в общий
 * компонент нельзя — она тянет за собой IndexedDB и код кабинета.
 */
export const QUEUED_PHOTO_PREFIX = "queued-photo:";
