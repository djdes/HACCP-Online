/**
 * Лайтбокс переехал в src/components/shared — им пользуются и Mini App,
 * и настройки набора журналов (превью бланков). Реэкспорт оставлен,
 * чтобы не трогать импорты в mini-страницах.
 */
export { PhotoLightbox } from "@/components/shared/photo-lightbox";
