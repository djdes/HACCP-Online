# Журналы → TasksFlow: Smart Routing → Sensors → Bonus

> Один большой проект разбит на три независимые фазы. Делается строго
> по порядку. Этот файл — единственный источник истины «где мы сейчас».

---

## ⚠️ Правила работы по этому плану

1. **Идём сверху вниз внутри фазы.** Не прыгаем.
2. **Пока в текущей фазе остался хоть один незавершённый пункт —
   следующую фазу не начинаем.**
3. **Завершённый пункт удаляется из файла** — не зачёркивается, а
   вырезается целиком (вместе с комментариями к нему). Это коммитится.
4. **Когда фаза становится пустой** (ни одного пункта не осталось) —
   удаляется заголовок фазы и её преамбула.
5. **Когда файл стал пустым** (нет ни одной фазы) — задача целиком
   завершена и файл `docs/journal-distribution-plan.md` удаляется.
6. **Перед стартом фазы**: все её 🔴 открытые вопросы должны иметь
   зафиксированные ответы прямо в этом файле. Если не зафиксированы —
   спросить пользователя и записать ответы перед кодом.
7. **Не открывать другие фичи** мимо этого плана пока он не пуст.
   Багфиксы и UX-полировка — можно (отдельные коммиты).

Чтобы продолжить в новом чате — просто скажи: «читай
docs/journal-distribution-plan.md и продолжай».

---

## Фаза 1 — Smart Routing (A + B)

> Решает главную боль: менеджер жмёт «отправить всё в TasksFlow», и
> сейчас одна задача уходит ВСЕМ. Цель фазы: каждому шаблону зашит
> режим распределения и список разрешённых должностей; обязательства
> создаются по этой логике, а не «всем подряд».

### ✅ Зафиксированные решения (2026-04-25)

- **Q1 → D**: `fillMode = "single"` поддерживает гибрид:
  если менеджер указал `defaultAssigneeId` — обязательство всегда
  идёт ему; иначе round-robin между сотрудниками с допустимой
  должностью.
- **Q2 → A**: настройки живут в `/settings/journals` — рядом с
  toggle «включить/отключить», в одной карточке per-template.
- **Q3 → A**: миграция через `seed-journal-fill-modes.ts` ставит
  дефолты: `hygiene`, `health_check`, `ppe_issuance` → `per-employee`;
  все остальные шаблоны → `single`. Менеджер правит руками.
- **Q4 → A**: «допустимые должности» — white-list per-org
  `JobPosition` через новую таблицу `JobPositionJournalAccess`.

### Шаги

- [ ] **1.5.** Переписать `bulk-assign-today` (и аналоги — найти все
  места где создаются `JournalObligation`):
  - Если `fillMode === "per-employee"` — старая логика, обязательство
    каждому из getEligibleEmployees
  - Если `fillMode === "single"` — одно обязательство на pickSingleAssignee
  - Если `fillMode === "sensor"` — не создаём ничего (Фаза 2 заменит)
- [ ] **1.6.** UI в `/settings/journals` (одно место — Q2.A):
  - На каждой карточке шаблона — селект `fillMode` (3 опции с пояснениями)
  - Под ним: чекбоксы должностей орги (Q4)
  - Если `single` — селект «по умолчанию назначать [имя или авто]»
- [ ] **1.7.** Migration script `prisma/seed-journal-fill-modes.ts`:
  - Для всех `JournalTemplate` без `fillMode` — выставить дефолт по
    хардкод-маппингу (Q3): hygiene/health/ppe → per-employee, остальное
    → single
  - Идемпотентно
- [ ] **1.8.** Ручной QA на проде по чек-листу
  - Менеджер выбрал `single` для cleaning — после bulk-assign только
    один сотрудник видит задачу
  - Менеджер выбрал `per-employee` для hygiene — все 5 поваров видят
  - Запретил уборщице фритюр — fryer-oil не приходит уборщице
- [ ] **1.9.** TasksFlow sync — проверить что переписанные obligations
  корректно создают TasksFlow-задачи через адаптеры

---

## Фаза 2 — Sensors / IoT (после Фазы 1)

> Цель: вычистить из штатных задач то что должен делать датчик
> (температура холодильников, влажность, УФ-лампа), сэкономить смене
> 5–10 минут в день.

### 🔴 Открытые вопросы (когда дойдём — спросить)

- [ ] **Q5.** Onboarding — менеджер вручную добавляет каждый датчик в
  /settings/equipment, или есть «discover devices» через tuya cloud
  по аккаунту орги?
- [ ] **Q6.** Что делать если датчик пропал (battery / сеть)? Падать
  на штатное обязательство автоматически, или дёргать менеджера?

### Шаги

- [ ] **2.1.** Audit существующего `src/lib/tuya.ts` — какие операции
  уже работают, что можно вызывать
- [ ] **2.2.** Schema: расширить `Equipment` — ссылки на template-fields
  которые этот датчик заполняет (`fillsTemplateId`, `fillsFieldKey`)
- [ ] **2.3.** Worker / cron: когда `fillMode === "sensor"`, вместо
  создания obligation — проверять последнее значение датчика; если
  свежее (≤ 1 час) — авто-создать `JournalEntry` или `JournalDocumentEntry`
- [ ] **2.4.** `/settings/equipment` — добавить «привязать датчик к
  журналу» в UI
- [ ] **2.5.** Failover (Q6): если датчик не отвечает > 24 часов —
  возвращать задачу штату (создаём obligation на день) + уведомление
  менеджеру

---

## Фаза 3 — Bonus + Photo Proof (после Фазы 2)

> Цель: мотивационная подсистема. Сверх-работа (журналы доп. бонусные)
> оплачивается отдельно, премия идёт первому-кто-выполнил, фото-доказ
> обязательно. Превращает заполнение в гонку — поэтому очень рискованно
> делать без хорошего фундамента из Фаз 1+2.

### 🔴 Открытые вопросы (когда дойдём — обсуждать долго)

- [ ] **Q7.** Размер бонуса — фиксированный per-template (как в идее,
  «+50₽ за журнал»), или менеджер ставит сам?
- [ ] **Q8.** Куда списывается бонус? Отдельная статья «премия по
  задачам» в payroll-выгрузке или просто учёт в кабинете?
- [ ] **Q9.** Manager approval после фото — обязательный (медленнее,
  но честнее) или auto-approve если фото есть (быстрее, но fraud-risk)?
- [ ] **Q10.** Что если двое одновременно отправили запись (race
  condition в DB)? Нужен `SELECT FOR UPDATE` + чёткое правило «выиграл
  тот чья транзакция первой коммитнулась», и UI «опоздал».

### Шаги (черновик, уточнится перед фазой)

- [ ] **3.1.** Schema: `BonusEntry { obligationId, amount, status,
  photoUrl, approvedById, approvedAt }`
- [ ] **3.2.** Per-template setting `bonusAmount` (Q7)
- [ ] **3.3.** API: `POST /api/journals/.../claim-bonus` с
  `SELECT FOR UPDATE` на `JournalObligation` — кто первый, тому
  обязательство и `BonusEntry pending`
- [ ] **3.4.** Photo upload pipeline (предположительно уже есть
  `JournalEntryAttachment` — переиспользовать)
- [ ] **3.5.** Manager-approval UI (Q9)
- [ ] **3.6.** Payroll-выгрузка / отчёт «Премии за период» (Q8)
- [ ] **3.7.** Анти-фрод: для photo проверять EXIF timestamp, deny
  если photo старше 5 минут или не сделано на устройстве

---
