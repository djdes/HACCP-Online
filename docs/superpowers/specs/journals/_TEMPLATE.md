# Journal `<code>` — `<Точное название>`

**Tier:** A (СанПиН-обязательный) / B (соц.учр. обязательный) / C (ХАССП-рекомендуемый)
**СанПиН:** Прил. N / п. N / N/A
**Status:** draft / approved / in-impl / live

## Назначение

Что фиксирует журнал и зачем (1-2 предложения).

## Колонки таблицы

Точные колонки в порядке как в нормативе. Каждая = field в `JournalTemplate.fields`.

| # | Название колонки | Тип | Обязательное | Нормативный пин |
|---|------------------|-----|--------------|-----------------|
| 1 | ... | text/number/date/select/photo/signature | да/нет | СанПиН прил. N графа M |

## Ритм заполнения

- **Frequency:** daily / weekly / monthly / event-driven / shift-driven
- **Per row:** один сотрудник / одно оборудование / одна партия / одно событие
- **Кто инициирует:** TasksFlow auto / manager manual / event trigger

## Роли (кто заполняет)

| Роль | Что делает |
|------|------------|
| Filler | пишет данные |
| Verifier | проверяет/одобряет |

## Pipeline (TF task) — если применимо

Шаги для wizard'а в TasksFlow Mini App:
1. Шаг N — описание + photo:required/optional/none
2. ...

## Compliance rule

Правило «заполнен сегодня» для дашборда и compliance ring.

## Edge cases / валидация

- Если поле X = «отклонение» → требуем поле Y (причина) обязательно
- Если ...

## TasksFlow flow

- Тип задач: 1 на смену / N (per-сотрудник) / N (per-оборудование) / event-only
- rowKey шаблон: ...
- Verifier: yes/no, кто

## Срок хранения

По СанПиН / внутренний регламент.

## Sources

- [Прямая ссылка на норматив]()
