Точки, третий круг: сводка по точкам, именование точек, «Общий», закрытие дня и превью по точкам, реквизиты и сотрудники точки, proxy вместо middleware

- Дашборд: сводка по точкам — заполнено сегодня на каждой, клик переключает точку
- Анкета: после «Готово» с 2+ точками — шаг «Назовите точки» (имена и адреса сразу)
- Списки документов: бейдж «Общий» у документов без точки рядом с документами точек (31 клиент)
- «Закрыть день без событий» и превью журналов — свои у каждой точки: buildingKey в уникальных ключах JournalCloseEvent/JournalPreview (миграция уже применена на прод)
- Точка в отчётах (графики и счётчики по документам активной точки), экспорте (buildings.json), инспекторской ссылке и её PDF, сводном ZIP
- Карточка точки: КПП и телефон (в шапку PDF), «Сотрудники точки» с мультивыбором (PUT /api/settings/buildings/[id]/staff)
- Mini App: сверка обязательств на главной идёт в фоне (ждём только первое открытие дня) — второй запрос ~100 мс вместо 2–3 с
- PageHeader: действия переносятся под заголовок, а не сжимают описание
- Корневой middleware.ts (только no-cache) перекрывал src/middleware.ts в dev — удалён; логика в src/proxy.ts (конвенция Next 16); guard партнёра «просмотр» теперь работает и на dev

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
