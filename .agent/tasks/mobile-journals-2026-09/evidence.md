# Evidence — мобильная проверка журналов (2026-09-05)

Dev `http://localhost:3020`, Chrome 390×780 (isMobile), тестовая организация
«Кафе «Тестовое 1»». Обход `e2e/crawl.ts`; сводки `e2e/crawl-results-f1.json`,
`crawl-results-f2.json` (финальный проход), `crawl-results-g.json` /
`crawl-results-h.json` (перепроверка после правок), десктоп бумажного журнала
`crawl-results-paper-desktop.json`, `shots-desktop/`.

## Найдено и исправлено

| # | Где | Что было | Правка |
|---|-----|----------|--------|
| 1 | Бумажный журнал (`/settings/journals/paper/[id]/documents/[docId]`) | Шапка бланка и таблица строк — два отдельных `overflow-x-auto`; на телефоне шапка уезжала отдельно от колонок; min-w 640 vs 720 | Один скроллер с `data-journal-doc-pan` (иначе blanket-правило globals.css делало скроллером каждый div), `w-fit min-w-full` — обе таблицы одной ширины. До: `same=false, scrollers=2`; после: `same=true, scrollers=1` (390) и `scrollers=0` (1280) |
| 2 | Чек-лист уборки и проветривания | Таблица в своём `max-h-[70vh] overflow-auto` — на телефоне отдельный скроллер от шапки | `max-sm:max-h-none max-sm:overflow-visible` |
| 3 | Контроль стекла, Перечень изделий, Акт забраковки, Чек-лист санитарного дня | Тумблер «Карточки / Таблица» внутри листа `min-w-[1100px]` → ширина тумблера 1100px, «Таблица» за экраном | Тумблер вынесен перед широким контейнером. Ширина тумблера после: 326–358px |
| 4 | Журнал аварий, Карточка поломок | `flex justify-between` сжимал H1 кнопками до столбика букв («Жу/рн/ал») | На мобильном заголовок и кнопки в колонку (`flex-col sm:flex-row`) |
| 5 | 21 журнал с `DocumentPageHeader` / `DocumentBackLink documentId` | Две кнопки «Печать» подряд (PDF и `window.print()`) | Печать страницы — иконка `size-9` с `aria-label="Печать страницы"`, как в `DocumentActionsBar` |
| 6 | `DocumentPageHeader` | На телефоне действия столбиком во всю ширину (3 кнопки = полэкрана) | Строка с переносом |
| 7 | `DocumentBackLink` (20 журналов) | Вторая ссылка «← Назад» под крошками | `showBack` по умолчанию `false` |

## Итог финального прохода (35 журналов + бумажный)

- Горизонтальное переполнение страницы: 0 из 36.
- Бумажная шапка и основная сетка в одном скроллере: у всех клиентов с шапкой,
  кроме прослеживаемости и чек-листа проветривания, где шапка лежит в
  `overflow-hidden`-карточке шириной экрана, но панорамируется вместе с
  сеткой (визуально не расходятся).
- Тумблер вида шире экрана: 0 (было 4).
- Диалог «Создать документ»: у всех 30 журналов с кнопкой помещается в 780px;
  6 диалогов высотой 756px прокручиваются внутри (`DialogContent` в
  `src/components/ui/dialog.tsx` уже имеет `max-h-[calc(100vh-2rem)] overflow-y-auto`).
  Замечание: у диалогов СИЗ / приёмки два крестика закрытия (свой + встроенный) — не трогал.
- Журналы без табличного вида (аудит: отчёт/план/протокол, план обучения,
  дезсредства) — форма/список, переполнения нет.

## Проверки

- `npm run typecheck` — зелёный.
- ESLint по изменённым файлам — 0 ошибок (только прежние warnings).
- Тестовые данные: бумажный документ «E2E …» удалён, `disabledJournalCodes`
  организации восстановлен (`e2e/disabled-codes.ts --restore`).

## Не сделано / на потом

- Диалоги журналов: свой крестик рядом со встроенным (СИЗ, приёмка, бракераж).
- Прослеживаемость: шапка бланка уже сетки на телефоне (карточка `overflow-hidden`).
- Скелетоны документов долго висят на dev (компиляция) — на проде не воспроизводится.

## Круг 2 — единая раскладка (2026-09-06)

Запрос: «сделать как в журнале уборка, гигиенический пример, вид табличный с
переключением в карточку… убрать разнобой везде, чтобы раскладка повторялась».

Новый компонент `src/components/journals/journal-document-shell.tsx` держит
канон, 24 журнала переведены на него (по 4-5 файлов параллельными агентами,
эталон миграции — мойка оборудования). Бумажная шапка везде — общий
`JournalDocumentHeader`, таблицы — токены `GRID_CELL_CLASS` /
`GRID_HEAD_CELL_CLASS`, кнопка «Добавить» — `DOC_PRIMARY_BUTTON_CLASS`.
У 8 «старых» журналов переключатель вида переехал из-под бланка под шапку.

### Замеры после (390×780, 35 журналов)

| Метрика | Значение |
|---|---|
| H1 (размер / насыщенность) | 28px / 700 во всех 35 |
| «Печать» + «Настройки журнала» + «⋯» в шапке | 1 + 1 + 1 во всех 35 |
| Вторая ссылка «Назад» | 0 |
| Ширина переключателя «Карточки / Таблица» | 358px (у одного 390px), нигде не шире экрана |
| Шапка бланка и таблица в одном скроллере | 34 из 35 (у чек-листа проветривания шапка в своей карточке, ширины совпадают) |
| Горизонтальное переполнение страницы | 0 |

Скриншоты: `shots/*-2-table.png`, контактные листы `unified-2-table.png`,
`unified-4-cards.png`, `toggle-moved.png`. Сводки —
`e2e/crawl-results-u1.json`, `u2`, `v1`.

### Побочно исправлено

- В журнале с отслеживанием подпись колонки «Дата» показывалась кракозяброй.
- Осиротевшее печатное правило `.sdc-header` в чек-листе санитарного дня.
- Гигиенический журнал и журнал здоровья получили подзаголовок с периодом.

## Круг 3 — всплывашки, инструкция, автозаполнение (2026-09-06)

Запрос: окно автосоздания не по центру и не скроллится, текста много;
«Инструкция» должна открывать обучение с двумя вкладками во всех журналах;
подписи под тумблерами убрать; тумблер автозаполнения — спрашивать «да/нет»
и при выключении возвращать как было.

### Причина «окно не по центру»

Полотно дашборда обёрнуто в full-bleed-блок с `translate: -50%` (Tailwind v4
пишет отдельное свойство `translate`, а не `transform`). Любой
`position: fixed` внутри считается от этой коробки: замер показал
`fixed inset-0` размером 390×1072 при экране 780 — карточка вставала по
центру страницы, а не экрана, и нижняя часть с кнопками уходила за край.
Проверка синтетическим `div` подтвердила, что блокировка прокрутки ни при чём.
Лечение — портал `ConfirmDialog` в `document.body` (так уже работало окно
«Как заполнить?»), на телефоне окно стало bottom-sheet.

| Замер (390×780) | До | После |
|---|---|---|
| `fixed inset-0` окна | 390×1072, top 73 | 390×780, top 0 |
| Карточка помещается в экран | нет (447…797) | да |

### Проверено

- Окно «Инструкция» открывается у журналов с топ-баром и без него
  (гигиена, климат, медкнижки, план аудитов, перечень изделий,
  прослеживаемость, уборка, дезинсекция): две вкладки, высота 702 из 780,
  прокрутка внутри.
- Всплывашки тумблеров: «Да, включить» / «Нет», помещаются в экран.
- Откат: `apply` → `revert` возвращает журнал в исходное состояние.
  Гигиена: создано 5 строк, удалено 5, счётчики вернулись к 82/82.
  Климат: изменена 1 клетка, восстановлена, 30/30.
- `npm test` 570 зелёных, typecheck и eslint чистые.

## Круг 4 — всплывашки на маленьком телефоне (2026-09-06)

Запрос: в партнёрском окне не видно крестика; продумать по всему сайту,
что сужать / где скролл, если экран маленький.

Причина: высота окон в `vh`. На iOS Safari `100vh` — высота БЕЗ адресной
строки, поэтому окно выше видимой части: у листа, прижатого к низу,
шапка с крестиком уходила за верхний край.

Канон (`src/components/ui/modal-tokens.ts`): высота в `dvh` с запасным
`vh`, карточка — колонка, шапка и подвал `shrink-0`, скроллится середина.
Базовый `DialogContent` держит крестик в липкой строке нулевой высоты.

### Замеры (карточка / крестик / главная кнопка / внутренний скролл)

| Окно | 320×568 | 375×600 |
|---|---|---|
| Партнёрская программа | помещается, ✕ виден, скролл внутри | то же |
| Инструкция журнала | помещается, ✕ виден, кнопка видна | то же |
| Создание документа | помещается, ✕ виден, кнопка видна | то же |
| Автосоздание журнала | помещается, ✕ виден, кнопка видна | то же |
| Добавление сотрудника | помещается, ✕ виден, кнопка видна | то же |
| Настройки журнала | помещается, один ✕ вместо двух | то же |
| Выключение автозаполнения | помещается целиком | то же |
| Обратная связь | помещается, ✕ виден, «Отправить» видна | то же |

Сводки: `e2e/modal-audit-320x568.json`, `e2e/modal-audit-375x600.json`,
снимки `shots-modals/`.

## Круг 5 — настройки, меню профиля, листы снизу (2026-09-06)

Запрос: тему — в общие настройки; меню профиля на телефоне вызывать
листом снизу, как в приложениях (и подумать, не заменить ли так все
всплывашки); страницу настроек сделать удобнее — показатели в строку,
блок «Быстрый старт» как на дашборде, разделы сгруппировать по логике.

### Сделано

| Что | Как |
|---|---|
| Тема | Новая страница «Настройки → Внешний вид»; из меню профиля подменю убрано |
| Меню профиля на телефоне | Лист снизу (`src/components/ui/bottom-sheet.tsx`): во всю ширину, крупные строки, крестик, Esc и тап по фону |
| Остальные окна на телефоне | Базовый `DialogContent` на узком экране — лист снизу со скруглением сверху |
| Показатели организации | Одна строка из четырёх плиток (нужен `no-mobile-shrink`: глобальное правило ломает `grid-cols-4` на телефоне) |
| «Быстрый старт» | Компактная строка «иконка · название · стрелка» на телефоне |
| Подтверждение почты | Кнопка ушла под текст, текст больше не сжимается в столбик |
| Группы разделов | Организация · Команда и доступы · Журналы · Задачи сотрудникам · Оплата · Интеграции и уведомления · Проверки и безопасность |

Проверка группировки скриптом: 42 карточки, каждая ровно в одной группе,
дублей нет, «Быстрая настройка» намеренно только большой кнопкой.

### Замеры

- Лист профиля (390×780): ширина 390 из 390, прижат к низу, есть «Внешний
  вид» и «Выйти».
- Настройки (390×780): показатели в одну строку, горизонтального
  переполнения нет, «Быстрый старт» 99px вместо ~200px.
- Восемь окон на 320×568 и на 1280×900: карточка помещается, крестик и
  главная кнопка видны. Крестик базового окна поднят над липкой шапкой —
  она его перекрывала.
