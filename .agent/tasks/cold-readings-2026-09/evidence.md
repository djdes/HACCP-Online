# cold-readings-2026-09 — доказательства

Дата: 2026-09-18. Стенд: `localhost:5432/wesetup_e2e`, `next dev -p 3020`, Playwright (десктоп и телефон с тачем).

| Проверка | Результат |
|---|---|
| `npx tsc --noEmit --skipLibCheck -p tsconfig.typecheck.json` | чисто |
| `node --import tsx --test src/lib/cold-equipment-document.test.ts` | 4/4 |
| `npx eslint` по файлам задачи | 0 ошибок |
| `e2e/cold-readings.ts` | 15/15 PASS (`e2e/cold-readings.json`, `shots/`) |
| регрессия `qr-a4-temperature-2026-09/e2e/poster-scan.ts` | 29/29 PASS |

- **AC1** — у морозилки минус в поле сразу, кнопка «−» переключает знак (тач), −18 по QR записано; у холодильника поле пустое.
- **AC2** — юнит-тесты слотов: ключи `id`, `id#2`, пустая строка и синхронизация, выбор слота для записи, отклонения по слотам.
- **AC3** — журнал: строки «1-й замер» и «2-й замер», у морозилки одна строка; значения двух сотрудников за дату
  видны вместе; правка 2-го замера сохраняется в запись-владельца, 1-й не тронут.
- **AC4** — QR: первый скан → 1-й замер, второй (другой сотрудник) → 2-й, третий обновляет последний.
- **AC5** — PDF: строки «1-й замер» и «2-й замер».
- **AC6** — «Что нового»: категория «Температура холодильников»; прод-смоук после деплоя.

Найдено и исправлено по ходу: после сохранения из состояния сетки выпадала запись второго сотрудника за ту же дату;
правка в сетке писала значение не в ту запись, где оно лежало.

Независимый верификатор не запускался: владелец просит скорость; проверка — свои e2e.
