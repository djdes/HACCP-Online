# Бумажные журналы — evidence

Дата проверки: 2026-09-03. Спецификация: `spec.md`. Коммит фичи: `4245d8c6`, SHA заметок: `682ab1fc`.

## Команды

| Команда | Результат |
|---|---|
| `npx prisma generate` + `npx prisma db push` (локальная БД) | схема применилась, новые колонки `dateFrom`, `dateTo`, `responsibleUserId`, `verifier`, `verifierUserId` |
| `npx tsc --noEmit --skipLibCheck` | 0 ошибок в `src/` |
| `npx eslint` по изменённым файлам | 0 ошибок, 0 предупреждений |
| `npm test` | 435/435 |
| e2e (Node Playwright, dev-сервер на 3031, `raw/results.json`) | 31/31 |

## Критерии приёмки

- **AC1 — PASS.** `prisma/schema.prisma`: пять новых nullable-полей у `PaperJournalDocument`; на проде `db push` прошёл, колонки есть (проверено `information_schema.columns`).
- **AC2 — PASS.** `src/lib/paper-journal-columns.ts`; в документе `electrical_safety` «ФИО работника» и «ФИО проверяющего» — разные люди (e2e). Отступление от ТЗ в пользу его же цели: в колонки «Подпись …» ничего не подставляется (раньше туда попадало ФИО), при этом `hasResponsibleColumn` считает и колонку подписи — у огнетушителей поле ответственного в модалке остаётся.
- **AC3 — PASS.** `create-paper-document-dialog.tsx`: автоназвание «Журнал вводного инструктажа по охране труда — сентябрь 2026», при смене даты начала на 15.09 — «15–30 сентября 2026»; каскад должность → сотрудник; у `electrical_safety` поле «Должность проверяющего» и нет поля ответственного; редирект на страницу документа (e2e).
- **AC4 — PASS.** Строка списка — ссылка на документ, подстрока «сентябрь 2026 · <ответственный> · Изменён …»; закрытие через `ConfirmDialog` переводит документ в «Закрытые» (e2e).
- **AC5 — PASS.** `paper-journal-workspace.tsx` удалён; `paper-journal-intro.tsx` на странице списка; черновик пустой при заходе, «Подставить сотрудников» наполняет 14 строк, PDF отдаётся 200 `application/pdf` (e2e).
- **AC6 — PASS.** `documents/[docId]/page.tsx` + `paper-document-header.tsx`: шапка с периодом и людьми, бейдж «Закрыт», поля `readonly` у закрытого; чужая организация и несуществующий id — 404 (e2e).
- **AC7 — PASS.** Редактор: в новом документе строки заполнены сотрудниками, «ФИО инструктирующего» = выбранный в модалке, «Дата» = 01.09.2026 (начало периода), подписи пустые; правка ячейки переживает перезагрузку (автосейв); селект «Кто проводит инструктаж» убран; подпись внизу зависит от режима.
- **AC8 — PASS.** `documents/route.ts` принимает `title/dateFrom/dateTo/responsible*/verifier*`, фолбэк названия — `buildJournalDocumentTitle`; `GET` отдаёт период и ответственного; `[docId]/route.ts` принимает `verifier`.
- **AC9 — PASS.** `paper-journal-pdf.ts` печатает «Период: …» при переданном периоде; `POST /pdf` с `dateFrom/dateTo` — 200 (e2e); черновик период не шлёт.
- **AC10 — PASS.** `demo-organization.ts` ставит `dateFrom/dateTo` на текущий месяц, `responsible`/`verifier` по наличию колонок; `paperJournalTitle` удалён; заметка в «Журналы», `LATEST_NOTES_BUILD_SHA = 4245d8c6` отдельным коммитом `682ab1fc`.
- **AC11 — PASS.** См. таблицу команд.

## Прод (после деплоя `682ab1fc`)

`.build-sha = 682ab1fc`, PM2 `haccp-online` online, `/` `/login` `/pricing` `/api/health` — 200, `/settings/journals/paper/ot_intro` и страница документа без сессии — 307 на `/login`; на лендинге 10 упоминаний СанПиН 2.3/2.4.4282-26 и ни одного 3590-20 как действующего.

## Известное, не относящееся к задаче

- В dev-консоли предупреждение о расхождении id при гидратации у radix-крошек в `PageNav` — старое, воспроизводится на любой странице кабинета.
