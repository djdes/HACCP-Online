# finished-product-entry-2026-09 — доказательства

Дата: 2026-09-17. Стенд: `localhost:5432/wesetup_e2e`, `next dev -p 3020`, Playwright Chromium (десктоп и телефон с
тачем). Прод — только смоук после деплоя.

## Команды

| Проверка | Результат |
|---|---|
| `npx tsc --noEmit --skipLibCheck -p tsconfig.typecheck.json` | чисто |
| `npx eslint` по файлам задачи | 0 ошибок |
| `e2e/finished-entry.ts` | 14/14 PASS (`e2e/finished-entry.json`, скриншоты `shots/`) |
| регрессия `journal-columns-2026-09/e2e/columns.ts` | 23/23 PASS |

## По критериям

- **AC1** — «Добавить списком»: окно с полем списка и общими полями; три строки получили оценку «Хорошо» и ФИО из
  окна, id разные (`shots/bulk-dialog.png`).
- **AC2** — `JournalCellInput`: подсказки под ячейкой, textarea остаётся в фокусе; выбор подсказки подставляет
  значение; ручной ввод сохраняется; на телефоне после тапа ячейка в фокусе, подсказки видны, ввод сохраняется
  (`shots/cell-suggestions.png`). При точном совпадении значения с вариантом показываются все варианты.
- **AC3** — окно записи: Select с четырьмя оценками и «Своя формулировка…» (сохраняется свой текст); ячейка таблицы:
  подсказки со стандартными оценками, выбор сохраняется (`shots/organoleptic-select.png`).
- **AC4** — «Что нового»: категория «Бракераж готовой продукции» (иконка `ClipboardCheck`).
- **AC5** — таблица команд; прод-смоук после деплоя (ниже).

Независимый верификатор не запускался: владелец просит скорость; проверка — свои e2e.
