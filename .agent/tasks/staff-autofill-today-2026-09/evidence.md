# staff-autofill-today-2026-09 — доказательства

Дата: 2026-09-16. Стенд: `localhost:5432/wesetup_e2e`, `next dev -p 3020`, Playwright. Прод — только чтение до деплоя.

## Диагноз на проде (только чтение, psql)

Организация «Одинцовская Лингвистическая Гимназия» → `cmtwzhw9700w73vts4z0z254u`, 18 сотрудников, 1 точка, 7 документов.
Активные «Журнал здоровья · Сентябрь с 16 по 30» и «Гигиенический журнал · Сентябрь с 16 по 30» (тумблер
автозаполнения включён): будущих ячеек с данными 224 и 238, и все они — ровно автоматические значения
(`{"signed": true, "measures": null}` и `{"status": "healthy", "temperatureAbove37": false}`), ручных данных нет.
Ежедневный cron (`journal.automation.run`, 03:00 МСК) создаёт по 24 ячейки за день — он не виноват; будущее
заполнили добавление сотрудников и включение тумблера.

Скрипт `repair-journal-responsibles.ts` (dry-run) по этой организации: «Проблем не найдено», `--apply` не нужен.

## Команды

| Проверка | Результат |
|---|---|
| `npx tsc --noEmit --skipLibCheck -p tsconfig.typecheck.json` | чисто |
| `node --import tsx --test src/lib/staff-journal-autofill.test.ts src/lib/building-scope.test.ts` | 9/9 |
| `npx eslint` по файлам задачи | 0 ошибок |
| `e2e/autofill-today.ts` (гигиена и здоровье: добавление сотрудника, «заполнить всех», тумблер, cron, права) | 15/15 PASS |

## По критериям

- **AC1** — `limitDateKeysToToday`; `applyStaffJournalAutoFill` отбрасывает даты после сегодня (юнит-тест).
- **AC2** — `add_employee`/`fill_from_list`: строки на весь период пустые, при включённом автозаполнении
  отметки до сегодня ставит общий helper; e2e: будущие дни пустые (14 дн.), прошлые и сегодня заполнены,
  ответ с `created` и `filled`.
- **AC3** — e2e: `apply_auto_fill` и cron не трогают будущее.
- **AC4** — «Что нового»: категория «Автозаполнение» (иконка `CalendarCheck`).
- **AC5** — прод: см. раздел «Очистка на проде» (дописывается после деплоя).
- **AC6** — таблица команд; прод-смоук после деплоя.

Независимый верификатор не запускался: владелец просит максимальную скорость; проверка — свои e2e.

## Очистка на проде (AC5, 2026-09-16 16:29 МСК, после деплоя a3ec0ddc)

Бэкап: `backups/journal-docs-entries-20260916-162925.dump` (pg_dump `-Fc`, таблицы `JournalDocument` и
`JournalDocumentEntry`, 1.9 МБ) в каталоге сайта на сервере. Затем в одной транзакции сброшены в `{}` ячейки двух
активных документов гимназии с датой > сегодня и данными, равными автоматическим значениям: гигиена 238, здоровье 224
(UPDATE 462). После: будущих заполненных ячеек 0, сегодняшних заполненных по 18 в каждом документе — не тронуты.
Прод-смоук после деплоя: 77/0.

## График выходных в «Журнале здоровья» (обращение ООО «Цветочек», 2026-09-17)

Автозаполнение здоровья не смотрело на график: подпись ставилась всем каждый день (гигиена учитывала выходные и
ставила статус «выходной»). Теперь график (недельные выходные, исключения, отпуска, больничные) загружается для обоих
журналов; в здоровье в такой день ячейка остаётся пустой. Юнит 3/3, e2e `autofill-today.ts` 17/17 (новый сценарий:
у повара сегодня выходной — гигиена «выходной», здоровье пусто, остальные заполнены). На проде у «Цветочка» отметок в
выходные ещё не было (график задан 17.09), чистить нечего.
