# Reviewed Visual Matrix

| Code | Local Ref | Live Bundle | List/Archive Ready | Detail Ready | Print Ready | Overall | Blocker |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `accident_journal` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `audit_plan` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `audit_protocol` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `audit_report` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `breakdown_history` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `cleaning` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `cleaning_ventilation_checklist` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `climate_control` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `cold_equipment_control` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `complaint_register` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `disinfectant_usage` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `equipment_calibration` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `equipment_cleaning` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `equipment_maintenance` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `finished_product` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `fryer_oil` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `general_cleaning` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `glass_control` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `glass_items_list` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `health_check` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `hygiene` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `incoming_control` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `incoming_raw_materials_control` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `intensive_cooling` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `med_books` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `metal_impurity` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `perishable_rejection` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `pest_control` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `ppe_issuance` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `product_writeoff` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `sanitary_day_control` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `staff_training` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `traceability_test` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `training_plan` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |
| `uv_lamp_runtime` | `PASS` | `PASS` (png=2 html=2 json=3) | `PASS` | `BLOCKED` | `BLOCKED` | `BLOCKED` | Full-crawl provides list/archive surfaces only; detail and print captures are still missing, and no row-by-row reviewed comparison has been recorded. |

## Notes

- Local reference inputs exist at repository scope for the 35-journal set via `journals/`.
- Full-crawl live bundles provide stable active/archive list surfaces for all mapped journals.
- This matrix separates evidence presence from actual reviewed visual verdicts, to avoid the usual ??????? ?looks close enough? fraud.